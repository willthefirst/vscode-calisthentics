/** 
 * Use command+d to select common strings 
 * - Replace all instances of "string" with your own string
 * - Refactor to use a single constant my replacing with an interpolated string
 */
export function doSomethingCool() {
  const a = "I am a delicious little string";
  const b = "I am another delicious little string";
  const c = "I am a delicious string with strings on top."
}